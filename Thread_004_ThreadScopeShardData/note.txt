线程范围内的共享变得概念和问题:

1，在Java开发中，有时会遇到这种情况，有一个全局变量，其中三个模块都需要去访问，
	在多个线程中，为了确保在同一线程内，其访问的是同一个变量，这时就要实现线程范围内的共享变量了
	【也就是说，即便是一个全局变量，在不同的线程中，其值是不一样的】
	【即在线程内多个模块共享数据，在线程外却保持数据独立】
	
	解决方案1：
		创建一个HashMap对象，其Key就是当前运行线程的Thread对象。线程内的不同模块获取数据用当前运行的Thread对象获取即可.
		
	解决方案2：
		使用ThreadLocal类也可以。